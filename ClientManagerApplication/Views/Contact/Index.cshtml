@model ClientManagerApplication.Models.Person

@{
    ViewBag.Title = "Index";
}

<h3>
    @Html.DisplayFor(model => model.first_name)
    @Html.DisplayFor(model => model.last_name)'s Contact Records
</h3>

<p>
    @Html.ActionLink("Add New Contact", "Create", new { id = Model.person_id })
</p>

@Html.Partial("ContactList", Model.Contacts)
@Html.ActionLink("Back to List", "Index", "Home")